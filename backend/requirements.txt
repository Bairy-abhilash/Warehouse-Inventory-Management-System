# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6

# Database ORM + driver
SQLAlchemy==2.0.35
psycopg2-binary==2.9.10

# Migrations
alembic==1.13.3

# Settings / validation
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0
python-dotenv==1.0.1

# Authentication
# bcrypt: password hashing (used directly, not via passlib to avoid
# compatibility bugs with modern bcrypt versions)
bcrypt==4.2.0
# python-jose: create and verify JSON Web Tokens
python-jose[cryptography]==3.3.0
# Needed by FastAPI's OAuth2 password flow
python-multipart==0.0.9